<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="icon" type="image/png" href="../images/logo-sm.png">
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <link rel="stylesheet" href="/stylesheets/font-awesome/css/font-awesome.min.css" />
    <title><%= user.username %> - profile</title>
</head>

<body>
    <div class="d-flex mx-auto flex-column ">
        <div class="row mt-5 text-center">
            <%- include('../partials/navbar') %>
          </div>

        <div class="row mt-5">
            <div class="col-12">
                <div class="card mb-3">
                <div class="card-body d-flex justify-content-center">
                    <div class="row">
                        <% if(user.gender == 'male'){ %> 
                           <img src="/images/maleNoProfile.jpg" class="profileImg rounded-circle mx-auto">
                        <% }else{ %> 
                            <img src="/images/femaleNoProfile.jpg" class="profileImg rounded-circle mx-auto">
                        <% } %> 
                        <div class="d-flex justify-content-center">
                            <i class="onlineIcon fa fa-circle"></i>
                            <p class="mx-1 h4"> <%= user.username %></p>
                        </div>
                    </div>
                </div>
                </div>
            </div>
        </div>

     
        <div class="card mb-3">
            <div class="row card-body">
                <div class="col-6 col-md-3">
                    <i class="fa fa-home fa-lg"></i>
                    <p class="h5"><%= user.city %></p>
                </div>
                <div class="col-6 col-md-3">
                    <% for(let i = 0; i < 5; i++){ %> 
                        <i class="starIcon fa fa-star fa-lg"></i>
                    <% } %> 
                    <p class="h5">Rate: 5</p>
                </div>
                <div class="col-6 col-md-3">
                    <i class="fa fa-cogs fa-lg "></i>
                    <p class="h5">Services: <%= user.service %></p>
                </div>
                <div class="col-6 col-md-3">
                    <i class="fa fa-calendar fa-lg "></i>
                    <% 
                        let today = user.joinedAt;
                        let year = today.getFullYear();
                        let month = today.getMonth()+1;
                        let day = today.getDate();
                        let date = day + '/' + month + '/' + year; 
                    %> 
                    <p class="h5">Joined: <%= date %></p>
                </div>                 
            </div>
        </div>
           

    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
 </body>
</html>